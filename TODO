Fix `asset-load-test`
Fix button not drawing
Properly implement double-buffering in the renderer/window
Make the renderer run in a separate thread

(Future) FUCKING WINDOWS SUPPORT
(Future) make proper OpenGL and Vulkan renderers
(Future) Split LOG_DEBUG into multiple levels
(Future) Implement lazy loading of menus in menu-mgr
(Future) Add popup menus
(Future) Add clicking sound in buttons
(Future) Add switch menu animations

(Far future) Add a file with instructions regarding the coding style
(Far future) Add the goddamn editor
(Far future) Move configuration to a config file opened and parsed during loading (at runtime) instead of config.h (at compile time)
