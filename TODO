Fix keyboard and mouse events being polled on every tick
(Almost done?) Add clicking animations in buttons

Get rid of the dependency on libpng
Restructurize the project
Refactor the modules so that they resemble actual APIs, not the garbage that they currently are
Add documentation in header files
Implement lazy loading of menus in menu-mgr

(Near future) Add popup menus
(Near future) Add hot code reloading with dlopen()
(Future) Add clicking sound in buttons, do NOT work with SDL_audio
(Future) implement switch menu animations

(Future) Add the goddamn editor
(Future) Move configuration to a config file opened and parsed during loading (at runtime) instead of config.h (at compile time)
(Far future) Move to OpenGL in favor of SDL2
