** Add the kb_destroyKeyboard() call at the end of main() **
** Locate memory leak (valgrind showed about 1500 bytes leaked!) **
Rewrite how event listeners listen in on their event
** Add free(oe_OnEvent*) where it is needed **
Perhaps rewrite the oe_OnEvent class altogether
Add clicking animations in buttons
Implement lazy loading of menus in menu-mgr
(Near future) Add popup menus
(Future) Add clicking sound in buttons, work with SDL_audio
(Future) implement switch menu animations